#server:
 # type: simple
  #applicationConnectors:
   # type: http
    #port: 8080

server:
  type: simple
  applicationContextPath: /application
  adminContextPath: /admin
  connector:
    type: http
    port: 8080